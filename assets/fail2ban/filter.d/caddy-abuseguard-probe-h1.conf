# fail2ban filter: HTTP/1.1 probing of sensitive paths on a protected site.
# The (abuseguard) snippet tags such requests with caddy_abuseguard_probe=web-probe.
[Definition]
failregex = ^(?=.*"client_ip":"<ADDR>")(?=.*"proto":"HTTP/1\.1")(?=.*"caddy_abuseguard_site":"protected")(?=.*"caddy_abuseguard_probe":"web-probe").*$
ignoreregex =
