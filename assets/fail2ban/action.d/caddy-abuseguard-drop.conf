# Block a banned source on every port and protocol, including SSH.
# Keep Fail2Ban's per-jail sets: unbanning in one jail must not undo another
# jail's still-active ban. All sets enforce the same source-address policy.
[INCLUDES]
before = nftables.conf

[Definition]
type = allports
rule_match-allports =

[Init]
blocktype = drop
